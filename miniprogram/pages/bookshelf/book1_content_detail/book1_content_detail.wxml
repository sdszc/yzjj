<!--pages/bookshelf/book1_content_detail/book1_content_detail.wxml-->
<view wx:for="{{full_text}}" wx:key="item">
  <view wx:if="{{item.tips}}">{{item.tips}}</view>
  <view class="title">{{item.original_text}}</view>
  <view>{{item.note}}</view>
</view>

